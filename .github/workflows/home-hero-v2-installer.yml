name: Install homepage hero v2

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install final homepage hero
        run: |
          echo "Installer placeholder"
